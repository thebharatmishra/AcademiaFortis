import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.parallelintersect.academiafortis",
  appName: "academiafortis",
  webDir: ".next/server/app",
};

export default config;
